"use client";import{useState}from"react";import{products,formatNaira}from"@/lib/products";export default function PaymentPlans(){const[product,setProduct]=useState(products[0].id);const[deposit,setDeposit]=useState(10000);const p=products.find(x=>x.id===product)!;const remaining=Math.max(0,p.price-deposit);return <div className="mx-auto max-w-5xl px-5 py-24 lg:px-8"><p className="text-xs uppercase tracking-[.3em] text-[#d7ad54]">Pay Small Small</p><h1 className="serif mt-3 text-5xl sm:text-6xl">Your dream hair, your payment pace.</h1><p className="mt-6 max-w-2xl leading-8 text-white/55">Choose selected hair and pay gradually. This calculator is a planning tool; payment terms are confirmed with D Luxury Hair Collection before an order is accepted.</p><div className="mt-10 grid gap-6 md:grid-cols-2"><div className="glass rounded-3xl p-7"><label className="text-xs uppercase tracking-[.18em] text-white/40">Choose product</label><select value={product} onChange={e=>setProduct(e.target.value)} className="mt-3 w-full rounded-xl border border-white/10 bg-black p-4">{products.map(x=><option key={x.id} value={x.id}>{x.name} — {formatNaira(x.price)}</option>)}</select><label className="mt-7 block text-xs uppercase tracking-[.18em] text-white/40">Amount to start with</label><input type="number" min={0} max={p.price} value={deposit} onChange={e=>setDeposit(Number(e.target.value))} className="mt-3 w-full rounded-xl border border-white/10 bg-black p-4"/></div><div className="glass rounded-3xl p-7"><p className="text-xs uppercase tracking-[.18em] text-white/40">Plan summary</p><h2 className="serif mt-4 text-3xl">{p.name}</h2><div className="mt-8 space-y-4 text-sm"><div className="flex justify-between"><span className="text-white/45">Product price</span><b>{formatNaira(p.price)}</b></div><div className="flex justify-between"><span className="text-white/45">Starting amount</span><b className="text-[#f4d58d]">{formatNaira(deposit)}</b></div><div className="flex justify-between border-t border-white/10 pt-4"><span>Remaining</span><b className="text-[#f4d58d]">{formatNaira(remaining)}</b></div></div><a href="https://wa.me/2348072895663" className="mt-8 block rounded-full bg-[#d7ad54] px-5 py-4 text-center text-xs font-bold uppercase tracking-[.16em] text-black">Confirm Plan on WhatsApp</a></div></div></div>}
