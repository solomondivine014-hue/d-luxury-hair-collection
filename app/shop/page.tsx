"use client";import{useMemo,useState}from"react";import{products,categories,formatNaira}from"@/lib/products";import ProductCard from"@/components/ProductCard";
export default function Shop(){const[q,setQ]=useState("");const[cat,setCat]=useState("All");const filtered=useMemo(()=>products.filter(p=>(cat==="All"||p.category===cat)&&(`${p.name} ${p.texture}`.toLowerCase().includes(q.toLowerCase()))),[q,cat]);return <div className="mx-auto max-w-7xl px-5 py-16 lg:px-8"><p className="text-xs uppercase tracking-[.3em] text-[#d7ad54]">The collection</p><h1 className="serif mt-3 text-5xl sm:text-6xl">Shop Luxury Hair</h1><div className="mt-10 flex flex-col gap-3 sm:flex-row"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search wigs, textures..." className="flex-1 rounded-full border border-white/10 bg-white/[.04] px-5 py-4 text-sm outline-none focus:border-[#d7ad54]"/><select value={cat} onChange={e=>setCat(e.target.value)} className="rounded-full border border-white/10 bg-black px-5 py-4 text-sm outline-none">{["All",...categories].map(x=><option key={x}>{x}</option>)}</select></div><p className="mt-8 text-xs text-white/40">{filtered.length} pieces available</p><div className="mt-6 grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{filtered.map(p=><ProductCard key={p.id} product={p}/>)}</div></div>}
